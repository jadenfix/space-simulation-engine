name: repair-3d3v-nonlinear-conditioning

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Repair execution is embedded in spacewind-assurance.'
